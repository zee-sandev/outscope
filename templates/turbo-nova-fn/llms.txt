# Turbo Nova Fn

Functional Nova 2.0 turborepo. Use `routes`, `access`, `defineHandlers`, and `handle`.

- Shared routes live in `packages/routes`.
- Shared schemas live in `packages/schemas`.
- API handlers live in `apps/api`.
- Do not generate contracts, operations, controllers, or `@Implement`.

